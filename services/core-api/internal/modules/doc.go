// Package modules is the parent namespace for TutorHub business modules.
//
// Each module owns its domain, application services, repositories, and
// transport adapters. Cross-module access must go through explicit interfaces
// or durable events instead of reaching into another module's persistence.
package modules
